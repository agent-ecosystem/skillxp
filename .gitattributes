# Keep text files LF everywhere so Windows checkouts (core.autocrlf) don't
# rewrite Go sources or wrapper scripts; CI runs the test suite on
# windows-latest.
* text=auto eol=lf
*.png binary
*.jpg binary
*.gif binary
